server:
  port: "8080"

spring:
  datasource:
    url: "${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3306/re_mgr}"
    username: "${SPRING_DATASOURCE_USERNAME:app}"
    password: "${SPRING_DATASOURCE_PASSWORD:secret}"
    driver-class-name: "com.mysql.cj.jdbc.Driver"
  security:
    oauth2:
      client:
        registration:
          google:
            client-id: "${GOOGLE_CLIENT_ID:559368435968-4vqpn2iodhjrb7f6goch6uc58fcckpub.apps.googleusercontent.com}"
            client-secret: "${GOOGLE_CLIENT_SECRET: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}"

mybatis:
  configuration:
    map-underscore-to-camel-case: true
  mapper-locations: "classpath*:/mybatis/sql/**/*.xml"

logging:
  level:
    root: INFO

re-mgr:
  users:
    - name: "admin"
      password: "admin"
      roles:
        - "ADMIN"
        - "USER"
    - name: "user"
      password: "user"
      roles:
        - "USER"